input PostCreation{
    text:String!
    tags:[String]!
}
type Post {
    comments: [Comment]
    text: String!
    tags: [String]
}

extend type Query{
    findPost(entityID:  String!):Post
}
extend type Mutation{
    createPost(postDetails: PostCreation!):Post!
}
